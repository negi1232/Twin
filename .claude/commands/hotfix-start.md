---
description: main から緊急修正用の hotfix ブランチを作成する
argument: ホットフィックス名（例: "45-crash-on-capture"）
---

# ホットフィックスブランチ開始

緊急修正のため、main から新しい hotfix ブランチを作成します。

## 手順

1. ワーキングツリーがクリーンか確認
2. origin から最新を fetch
3. `main` をチェックアウトし、最新を pull
4. `hotfix/$ARGUMENTS` ブランチを作成してチェックアウト
5. 作成したブランチ名をユーザーに報告

## ルール
- ブランチ名の形式: `hotfix/<引数>`
- 必ず最新の `main` から分岐する
- 未コミットの変更がある場合は中断する
