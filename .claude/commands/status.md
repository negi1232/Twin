---
description: プロジェクトと Git の状態を一覧表示する
---

# プロジェクトステータス

現在のプロジェクトと Git の状態を概要表示します。

## 手順

1. 現在の Git ブランチを表示
2. Git ステータス（変更・未追跡ファイル）を表示
3. 直近5件のコミットログを一行形式で表示
4. `npm test -- --no-coverage --silent` を実行し、テストの合否数を報告
5. `npm run lint -- --quiet` で ESLint のエラーがあれば報告
6. すべてを簡潔にまとめて表示する
