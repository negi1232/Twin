name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: read

jobs:
  e2e-test:
    name: E2E Tests
    if: github.event.pull_request.merged == true || startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build:ts
      - run: npx playwright install --with-deps chromium
      - run: xvfb-run --auto-servernum npm run test:e2e

  build:
    name: Build
    if: github.event.pull_request.merged == true || startsWith(github.ref, 'refs/tags/v')
    needs: e2e-test
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Build (macOS)
        if: runner.os == 'macOS'
        run: npm run build:mac -- --publish never
      - name: Build (Linux)
        if: runner.os == 'Linux'
        run: npm run build:linux -- --publish never
      - name: Build (Windows)
        if: runner.os == 'Windows'
        run: npm run build:win -- --publish never
      - name: Upload artifacts
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: build-${{ matrix.os }}
          path: dist-release/

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/download-artifact@70fc10c6e5e1ce46ad2ea6f2b72d43f7d47b13c3 # v8.0.0
        with:
          path: artifacts/
      - name: Collect release assets
        run: |
          mkdir -p release-assets
          cp artifacts/build-macos-latest/*.dmg release-assets/ 2>/dev/null || true
          cp artifacts/build-macos-latest/*.zip release-assets/ 2>/dev/null || true
          cp artifacts/build-ubuntu-latest/*.AppImage release-assets/ 2>/dev/null || true
          cp artifacts/build-ubuntu-latest/*.deb release-assets/ 2>/dev/null || true
          cp artifacts/build-ubuntu-latest/*.snap release-assets/ 2>/dev/null || true
          cp artifacts/build-windows-latest/*.exe release-assets/ 2>/dev/null || true
          ls -la release-assets/
      - name: Create Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF_NAME}"
          gh release create "$TAG" \
            --title "Twin $TAG" \
            --generate-notes \
            release-assets/*
